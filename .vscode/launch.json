{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug SEQ",
      "cargo": {
        "args": ["build", "--bin=seq", "--package=seq"],
        "filter": {
          "name": "seq",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
